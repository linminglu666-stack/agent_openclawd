# 深度理解训练系统 v6.0 完整部署

## 系统架构

```
定时调度器 (每小时)
    │
    ▼
监控子代理
    │
    ├── 检查当前训练状态
    │   ├── 有进行中的任务 → 跳过
    │   └── 无进行中的任务 → 创建新训练
    │
    └── 故障恢复
        ├── 超时检测 (10分钟)
        └── 自动重试
            │
            ▼
    深度理解训练子代理
        │
        ├── 50轮渐进式理解
        ├── 最高级别思考 (thinking=high)
        ├── 防造假验证
        └── 结果 → 记忆系统
```

## 部署状态

### ✅ 已配置组件

| 组件 | 状态 | 路径 |
|------|------|------|
| 训练清单 | ✅ | `training/deep_understanding/training_manifest.json` |
| 监控器 | ✅ | `agent_openclawd/11_scripts/deep_understanding_monitor.py` |
| 定时任务 | ✅ | `deep-understanding-monitor` (每小时) |
| 执行指南 | ✅ | `training/deep_understanding/execution_guide.md` |

### 📋 训练任务

- **Plan数量**: 6 个核心进化型Plan
- **每Plan轮数**: 50 轮深度理解
- **总任务数**: 300 个训练任务
- **预计总时间**: 15 小时

## 工作原理

### 1. 定时检查 (每小时)

```python
# 监控器执行逻辑
def check_and_dispatch():
    if has_running_training():
        # 检查是否超时
        if is_timeout():
            mark_failed_and_retry()
        else:
            skip("训练进行中")
    else:
        # 获取下一个任务
        next_task = get_next_pending_task()
        if next_task:
            dispatch_training_subagent(next_task)
        else:
            log("所有任务完成！")
```

### 2. 防造假机制

每个训练任务必须通过验证：
- ✅ Token消耗 > 100
- ✅ 执行时间 > 10秒
- ✅ 内容长度 > 100字符
- ✅ 包含思考痕迹

### 3. 故障恢复

- **超时检测**: 超过10分钟视为超时
- **自动重试**: 失败后自动重新调度
- **状态持久化**: 所有状态保存到文件

## 监控命令

### 查看训练状态

```bash
# 查看监控日志
tail -f training/deep_understanding/monitor.log

# 查看状态文件
cat training/deep_understanding/monitor_state.json

# 查看调度器状态
openclaw scheduler status
```

### 手动触发训练

```bash
# 立即执行一次监控周期
python3 training/deep_understanding/training_monitor.py

# 手动启动特定Plan的训练
python3 training/deep_understanding/execute_training.py
```

## 结果存储

### 训练结果

```
training/deep_understanding/
├── memory_integration/     # 整合到记忆系统的理解
│   ├── P002_R01.json
│   ├── P002_R02.json
│   └── ...
├── results/                # 训练结果汇总
│   ├── P002_summary.json
│   └── ...
└── monitor.log            # 监控日志
```

### 记忆整合

训练结果会自动整合到：
- `MEMORY.md` - 长期记忆
- `SOUL.md` - 认知模型
- `memory/` - 详细记忆文件

## 配置调整

### 修改训练频率

```bash
# 编辑定时任务（需要重启调度器）
python3 -c "
from scheduler import get_scheduler
scheduler = get_scheduler()
scheduler.remove_schedule('deep-understanding-monitor')
# 重新添加，修改 cron 表达式
"
```

### 修改超时时间

编辑 `training/deep_understanding/training_monitor.py`：
```python
TIMEOUT_SECONDS = 600  # 默认10分钟
```

## 故障排查

### 问题1: 训练不启动

检查：
1. 调度器是否运行：`systemctl status openclawd-scheduler`
2. 监控器脚本权限：`ls -la 11_scripts/deep_understanding_monitor.py`
3. 状态文件：`cat training/deep_understanding/monitor_state.json`

### 问题2: 验证失败

原因：
- Token消耗不足
- 执行时间过短
- 输出内容过短

解决：确保使用 `--thinking=high` 模式

### 问题3: 任务卡住

监控器会自动检测超时（10分钟），标记失败后重试。

## 下一步

系统已完全自动运行：
- ✅ 每小时自动检查
- ✅ 自动创建训练任务
- ✅ 自动故障恢复
- ✅ 结果自动整合到记忆

**无需人工干预，系统将自动完成300个深度理解训练任务！**

---
*深度理解训练系统 v6.0 - 防造假版本*
